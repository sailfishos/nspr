diff --git a/mozilla/nsprpub/config/nspr-config.in b/mozilla/nsprpub/config/nspr-config.in
index a7d2b4f..2f441be 100755
--- a/mozilla/nsprpub/config/nspr-config.in
+++ b/mozilla/nsprpub/config/nspr-config.in
@@ -98,7 +98,7 @@ if test -z "$includedir"; then
     includedir=@includedir@
 fi
 if test -z "$libdir"; then
-    libdir=@libdir@
+    libdir=`pkg-config --variable=libdir nspr`
 fi
 
 if test "$echo_prefix" = "yes"; then
@@ -132,12 +132,12 @@ if test "$echo_libs" = "yes"; then
       if test -n "$lib_nspr"; then
 	libdirs="$libdirs -lnspr${major_version}"
       fi
-      os_ldflags="@LDFLAGS@"
+      os_ldflags=`pkg-config --variable=ldflags nspr`
       for i in $os_ldflags ; do
 	if echo $i | grep \^-L >/dev/null; then
 	  libdirs="$libdirs $i"
         fi
       done
-      echo $libdirs @OS_LIBS@
+      echo $libdirs `pkg-config --variable=os_libs nspr`
 fi      
 
diff --git a/mozilla/nsprpub/configure b/mozilla/nsprpub/configure
index f06d9dd..043e34e 100755
--- a/mozilla/nsprpub/configure
+++ b/mozilla/nsprpub/configure
@@ -6694,6 +6694,8 @@ MAKEFILES="
     config/nsprincl.mk
     config/nsprincl.sh
     config/nspr-config
+    config/nspr-config-pc
+    config/nspr-config-vars
     lib/Makefile
     lib/ds/Makefile
     lib/libc/Makefile
diff --git a/mozilla/nsprpub/configure.in b/mozilla/nsprpub/configure.in
index 112ddd2..9feebe0 100644
--- a/mozilla/nsprpub/configure.in
+++ b/mozilla/nsprpub/configure.in
@@ -3357,6 +3357,8 @@ MAKEFILES="
     config/nsprincl.mk
     config/nsprincl.sh
     config/nspr-config
+    config/nspr-config-pc
+    config/nspr-config-vars
     lib/Makefile
     lib/ds/Makefile
     lib/libc/Makefile
